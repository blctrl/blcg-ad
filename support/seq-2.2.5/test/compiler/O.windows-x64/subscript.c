/* C code for program subscriptTest, generated by snc from ../subscript.st */
#include <string.h>
#include <stddef.h>
#include <stdio.h>
#include <limits.h>

#include "seq_snc.h"

/* Variable declarations */
# line 9 "../subscript.st"
static	float single[2];
# line 12 "../subscript.st"
static	float multiple3[3];
# line 15 "../subscript.st"
static	int multiple2[2];


/* Function declarations */

#define seqg_var (*(struct seqg_vars *const *)seqg_env)

/* Program init func */
static void seqg_init(PROG_ID seqg_env)
{
}

/****** Code for state "test" in state set "test" ******/

/* Event function for state "test" in state set "test" */
static seqBool seqg_event_test_0_test(SS_ID seqg_env, int *seqg_ptrn, int *seqg_pnst)
{
# line 22 "../subscript.st"
	if (FALSE)
	{
		*seqg_pnst = 0;
		*seqg_ptrn = 0;
		return TRUE;
	}
	return FALSE;
}

/* Action function for state "test" in state set "test" */
static void seqg_action_test_0_test(SS_ID seqg_env, int seqg_trn, int *seqg_pnst)
{
	switch(seqg_trn)
	{
	case 0:
		{
# line 23 "../subscript.st"
			multiple2[0] = 0;
		}
		return;
	}
}

#undef seqg_var

/************************ Tables ************************/

/* Channel table */
static seqChan seqg_chans[] = {
	/* chName, offset, varName, varType, count, eventNum, efId, monitored, queueSize, queueIndex */
	{"", (size_t)&single, "single", P_FLOAT, 2, 1, 0, 0, 0, 0},
	{"x", (size_t)&multiple3[0], "multiple3[0]", P_FLOAT, 1, 2, 0, 0, 0, 0},
	{"y", (size_t)&multiple3[1], "multiple3[1]", P_FLOAT, 1, 3, 0, 0, 0, 0},
	{"", (size_t)&multiple3[2], "multiple3[2]", P_FLOAT, 1, 4, 0, 0, 0, 0},
	{"", (size_t)&multiple2[0], "multiple2[0]", P_INT, 1, 5, 0, 1, 0, 0},
	{"", (size_t)&multiple2[1], "multiple2[1]", P_INT, 1, 6, 0, 1, 0, 0},
};

/* Event masks for state set "test" */
static const seqMask seqg_mask_test_0_test[] = {
	0x00000000,
};

/* State table for state set "test" */
static seqState seqg_states_test[] = {
	{
	/* state name */        "test",
	/* action function */   seqg_action_test_0_test,
	/* event function */    seqg_event_test_0_test,
	/* entry function */    0,
	/* exit function */     0,
	/* event mask array */  seqg_mask_test_0_test,
	/* state options */     (0)
	},
};

/* State set table */
static seqSS seqg_statesets[] = {
	{
	/* state set name */    "test",
	/* states */            seqg_states_test,
	/* number of states */  1
	},
};

/* Program table (global) */
seqProgram subscriptTest = {
	/* magic number */      2002005,
	/* program name */      "subscriptTest",
	/* channels */          seqg_chans,
	/* num. channels */     6,
	/* state sets */        seqg_statesets,
	/* num. state sets */   1,
	/* user var size */     0,
	/* param */             "",
	/* num. event flags */  0,
	/* encoded options */   (0 | OPT_CONN | OPT_NEWEF),
	/* init func */         seqg_init,
	/* entry func */        0,
	/* exit func */         0,
	/* num. queues */       0
};

#define PROG_NAME subscriptTest
#include "seqMain.c"

/* Register sequencer commands and program */
#include "epicsExport.h"
static void subscriptTestRegistrar (void) {
    seqRegisterSequencerCommands();
    seqRegisterSequencerProgram (&subscriptTest);
}
epicsExportRegistrar(subscriptTestRegistrar);
